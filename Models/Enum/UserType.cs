namespace BancoCem.Models.Enum
{
    public enum UserType
    {
        Admin = 1, // Administrator with full access
        Developer = 2, // Developer with access to development tools and features
        User = 3, // Regular user with standard access
        Guest = 4, // Guest user with limited access
    }
}
